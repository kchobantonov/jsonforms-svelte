{
  "name": "jsonforms-svelte-monorepo",
  "engines": {
    "node": "^22.0.0",
    "pnpm": ">=10.28.2"
  },
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter jsonforms-svelte-flowbite-demo dev",
    "build": "pnpm -r build",
    "build:libs": "pnpm --filter @chobantonov/jsonforms-svelte build && pnpm --filter @chobantonov/jsonforms-svelte-flowbite build",
    "build:demo": "pnpm --filter jsonforms-svelte-flowbite-demo build",
    "preview": "pnpm --filter jsonforms-svelte-flowbite-demo preview",
    "clean": "pnpm -r exec rm -rf dist .svelte-kit node_modules",
    "install:all": "pnpm install"
  },
  "devDependencies": {
    "@sveltejs/kit": "^2.5.0",
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "svelte": "^5.1.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
